# PAUSED (2026-05-21): Use Cloudflare Dashboard Git → Pages with empty deploy command instead.
# Re-enable when CLOUDFLARE_API_TOKEN has Pages Edit + secrets are configured.
name: Deploy Cloudflare Pages

on:
  workflow_dispatch:

concurrency:
  group: cloudflare-pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Cloudflare Pages (edge build) — disabled
    if: false
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
      deployments: write
    steps:
      - run: echo "Workflow paused — deploy via Cloudflare Pages dashboard (no wrangler deploy command)."
